<div class="form-group"><label>Delegator</label><select name="delegator_id" class="form-control" required><option value="">-- Pilih --</option>@foreach($users as $user)<option value="{{ $user->id }}" {{ (string) old('delegator_id', optional($item)->delegator_id) === (string) $user->id ? 'selected' : '' }}>{{ $user->name }}</option>@endforeach</select></div>
<div class="form-group"><label>Delegate</label><select name="delegate_id" class="form-control" required><option value="">-- Pilih --</option>@foreach($users as $user)<option value="{{ $user->id }}" {{ (string) old('delegate_id', optional($item)->delegate_id) === (string) $user->id ? 'selected' : '' }}>{{ $user->name }}</option>@endforeach</select></div>
<div class="form-group"><label>Scope</label><select name="scope" class="form-control" required><option value="leave_approval" {{ old('scope', optional($item)->scope) === 'leave_approval' ? 'selected' : '' }}>Approval Cuti</option><option value="document_verification" {{ old('scope', optional($item)->scope) === 'document_verification' ? 'selected' : '' }}>Verifikasi Dokumen</option><option value="ppk_approval" {{ old('scope', optional($item)->scope) === 'ppk_approval' ? 'selected' : '' }}>Persetujuan PPK</option></select></div>
<div class="form-row"><div class="form-group col-md-6"><label>Mulai</label><input type="date" name="start_date" class="form-control" value="{{ old('start_date', optional(optional($item)->start_date)->toDateString()) }}" required></div><div class="form-group col-md-6"><label>Sampai</label><input type="date" name="end_date" class="form-control" value="{{ old('end_date', optional(optional($item)->end_date)->toDateString()) }}" required></div></div>
<div class="form-group"><label>Catatan</label><textarea name="note" class="form-control" rows="3">{{ old('note', optional($item)->note) }}</textarea></div>
<div class="form-check"><input type="checkbox" class="form-check-input" name="is_active" id="delegationActive{{ optional($item)->id ?: 'new' }}" {{ old('is_active', optional($item)->is_active ?? true) ? 'checked' : '' }}><label class="form-check-label" for="delegationActive{{ optional($item)->id ?: 'new' }}">Aktif</label></div>
